bool compare(string &s1,string &s2)
{
    return s1.size() < s2.size();
}

//sort(s, s+n, compare)
//s is string array
//n is string array length
// compare is a function to compare the size and many more
